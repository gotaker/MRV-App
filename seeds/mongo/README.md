# Mongo Seed Folder

Place Mongo initialization scripts here (e.g., `*.js`) **or** a `dump/` directory for `mongorestore`.

If you have BSON dumps from the legacy app (files under `Frontend/Misc/*.bson`), convert them into a standard `mongodump` layout or write a `*.js` script to insert them.
