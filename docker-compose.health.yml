# docker-compose.health.yml — optional overlay to add a healthcheck for web
services:
  web:
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- ${HEALTH_URL:-http://localhost:4200} >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 60
      start_period: 20s
